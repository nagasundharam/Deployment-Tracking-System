services:
  # Backend Service (Node.js/Express)
  backend:
    build: 
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://nagasm002_db_user:kKKyNwkpxtqdV9aB@cluster0.pslx0yb.mongodb.net/
      - PORT=5000
      - JWT_SECRET=your_jwt_secret_key
      - NODE_ENV=production
    restart: always

  # Frontend Service (React/Vite)
  frontend:
    build: 
      context: ./Frontend/deployment-tracker
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=http://34.204.195.105:5000/api
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always